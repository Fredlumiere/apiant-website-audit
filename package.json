{
  "name": "apiant-website-audit",
  "version": "1.0.0",
  "description": "Scheduled Lighthouse + SEO audit of apiant.com, published to Vercel behind a Basic-Auth gate",
  "private": true,
  "scripts": {
    "audit": "unlighthouse-ci --build-static",
    "build": "echo 'Static output already in ./dist'"
  },
  "devDependencies": {
    "@unlighthouse/cli": "^0.17.0"
  }
}
